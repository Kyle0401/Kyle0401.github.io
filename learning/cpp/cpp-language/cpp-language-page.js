(function () {
    'use strict';

    var previousFetch = window.fetch;

    var chapter13Section = [
        '#### 13.1 `std::array`：固定大小连续容器',
        '',
        '`std::array<T, N>` 是 C++11 提供的固定大小容器，定义在 `<array>` 中。它保存恰好 `N` 个 `T` 类型元素，元素在内存中连续存储，并提供和其他标准容器相似的接口。',
        '',
        '```cpp',
        '#include <array>',
        '',
        'std::array<int, 5> arr{{1, 2, 3, 4, 5}};',
        '```',
        '',
        '这里 `int` 是元素类型，`5` 是数组长度，同时也是模板参数的一部分，因此 `std::array<int, 5>` 与 `std::array<int, 6>` 是不同类型。',
        '',
        '##### 13.1.1 `size()`：取得元素个数',
        '',
        '`size()` 返回容器中保存的**元素个数**：',
        '',
        '```cpp',
        'std::array<int, 5> arr{{1, 2, 3, 4, 5}};',
        'arr.size();  // 5',
        '```',
        '',
        '需要特别区分 `arr.size()` 与 `sizeof(arr)`：',
        '',
        '| 表达式 | 含义 | 本例结果 |',
        '| --- | --- | --- |',
        '| `arr.size()` | 元素数量 | `5` |',
        '| `sizeof(arr)` | 整个 `std::array` 对象占用的字节数 | `5 * sizeof(int)` |',
        '',
        '`size()` 的单位是“元素”，而 `sizeof` 的单位是“字节”。',
        '',
        '##### 13.1.2 `data()`：取得连续元素的首地址',
        '',
        '`std::array` 本身是容器对象，不会像内置数组那样在普通表达式中自动退化成元素指针。需要把底层连续元素交给只接受指针的接口时，可以调用 `data()`。',
        '',
        '```cpp',
        'std::array<int, 5> arr{{1, 2, 3, 4, 5}};',
        'int* p = arr.data();',
        '```',
        '',
        '对于非 `const std::array<int, 5>`，`arr.data()` 返回 `int*`；对于 `const` 对象则返回 `const int*`。当数组非空时，可以把它理解为 `arr.data() == &arr[0]`。',
        '',
        '例如：',
        '',
        '```cpp',
        'std::array<int, 5> arr{{1, 2, 3, 4, 5}};',
        'int ans[]{1, 2, 3, 4, 5};',
        '',
        'ASSERT(std::memcmp(arr.data(), ans, 5 * sizeof(int)) == 0,',
        '       "The object representations should match.");',
        '```',
        '',
        '`std::memcmp` 的第三个参数单位是**字节**而不是元素个数，所以 5 个 `int` 对应 `5 * sizeof(int)` 字节。',
        '',
        '> [!WARNING]',
        '> `memcmp` 比较的是对象表示（原始字节序列），不是 C++ 意义上的通用“值相等”操作。普通标准容器的值比较通常优先使用 `operator==`。',
        '',
        '参考：[cppreference：`std::array`](https://en.cppreference.com/w/cpp/container/array)、[cppreference：`std::array::size`](https://en.cppreference.com/w/cpp/container/array/size)、[cppreference：`std::array::data`](https://en.cppreference.com/w/cpp/container/array/data)。',
        '',
        '#### 13.2 `std::vector`：动态连续容器',
        '',
        '`std::vector<T>` 是动态大小的连续容器。与 `std::array` 的固定长度不同，`vector` 可以在运行时增加或删除元素；它会自行管理一块连续的动态存储区域。',
        '',
        '```cpp',
        '#include <vector>',
        '',
        'std::vector<int> vec{1, 2, 3, 4, 5};',
        '```',
        '',
        '`vector` 最容易混淆的是“元素个数”“已分配容量”和“vector 对象自身大小”这三个概念。',
        '',
        '##### 13.2.1 `size()` / `empty()` / `capacity()`：元素数量与容量',
        '',
        '`size()` 返回当前实际存在的元素个数：',
        '',
        '```cpp',
        'std::vector<int> vec{1, 2, 3, 4, 5};',
        'vec.size();  // 5',
        '```',
        '',
        '`empty()` 用来判断容器是否为空，返回 `bool`。从含义上可以理解为检查 `size() == 0`：',
        '',
        '```cpp',
        'vec.empty();',
        '',
        'if (vec.empty()) {',
        '    // 当前没有元素',
        '}',
        '```',
        '',
        '`capacity()` 返回当前已经分配的存储空间最多可以容纳多少个元素，而无需再次重新分配内存。始终有：',
        '',
        '```text',
        'size() <= capacity()',
        '```',
        '',
        '例如一个 `vector` 可能处于：',
        '',
        '```text',
        'size     = 5',
        'capacity = 8',
        '',
        '已使用： [1][2][3][4][5]',
        '预留：                  [ ][ ][ ]',
        '```',
        '',
        '这表示当前有 5 个元素，但现有存储区域最多还能直接容纳到 8 个元素。',
        '',
        '还要把 `sizeof(vec)` 与它们区分开：`sizeof(vec)` 测量的是 **`std::vector` 管理对象自身占用的字节数**，并不等于底层动态元素区域占用的字节数，也不会随着 `size()` 简单地成比例变化。不同标准库实现的 `sizeof(std::vector<T>)` 也不要求相同。',
        '',
        '| 表达式 | 含义 | 单位 |',
        '| --- | --- | --- |',
        '| `vec.size()` | 当前实际元素数量 | 元素 |',
        '| `vec.empty()` | 当前是否没有元素 | `bool` |',
        '| `vec.capacity()` | 不重新分配时当前最多可容纳的元素数量 | 元素 |',
        '| `sizeof(vec)` | `vector` 管理对象自身占用空间 | 字节 |',
        '',
        '> [!IMPORTANT]',
        '> `size()` 描述“现在有几个元素”，`capacity()` 描述“当前这块存储空间最多能放几个元素”。两者不是同一个概念。',
        '',
        '##### 13.2.2 `data()`：取得底层连续存储的首地址',
        '',
        '`std::vector` 的元素连续存储，因此 `data()` 可以取得底层元素存储区域的起始地址。',
        '',
        '```cpp',
        'std::vector<int> vec{1, 2, 3, 4, 5};',
        'int* p = vec.data();',
        '```',
        '',
        '对于这个非 `const std::vector<int>`，`vec.data()` 返回 `int*`。当 `vec` 非空时，可以把它理解为：',
        '',
        '```cpp',
        'vec.data() == &vec[0]',
        '```',
        '',
        '因此可以把 `vector` 底层连续元素交给需要原始指针的接口：',
        '',
        '```cpp',
        'std::vector<int> vec{1, 2, 3, 4, 5};',
        'int ans[]{1, 2, 3, 4, 5};',
        '',
        'ASSERT(std::memcmp(vec.data(), ans, sizeof(ans)) == 0,',
        '       "The object representations should match.");',
        '```',
        '',
        '这里 `vec.data()` 是第一块连续内存的首地址，`ans` 在函数调用中退化为指向首元素的指针，`sizeof(ans)` 则给出整个内置数组的字节数。',
        '',
        '> [!IMPORTANT]',
        '> `vec.data()` 指向的是 **vector 管理的元素存储区域**，不是 `std::vector` 管理对象自身的地址。发生重新分配后，之前取得的 `data()` 指针可能失效。',
        '',
        '##### 13.2.3 `insert()` / `erase()`：按迭代器位置插入和删除',
        '',
        '`std::vector::insert()` 和 `std::vector::erase()` 使用**迭代器**表示操作位置，而不是直接传入数组下标。`std::vector` 的迭代器支持随机访问，因此可以使用 `vec.begin() + n` 得到下标 `n` 对应位置的迭代器。',
        '',
        '假设当前容器为：',
        '',
        '```cpp',
        'std::vector<double> vec{1, 2, 3, 4, 6};',
        '```',
        '',
        '要在值 `2` 的前面插入 `1.5`，可以写：',
        '',
        '```cpp',
        'vec.insert(vec.begin() + 1, 1.5);',
        '```',
        '',
        '这里 `vec.begin() + 1` 指向原来的第 1 个元素 `2`，而 `insert(position, value)` 会把新元素插入到 `position` **之前**。',
        '',
        '插入位置可以这样看：',
        '',
        '```text',
        'vec.begin() + 1 指向这里',
        '                    ↓',
        '原来：          1 | 2 | 3 | 4 | 6',
        '插入位置：      1 | ↑ | 2 | 3 | 4 | 6',
        '                    1.5',
        '',
        '结果：          1 | 1.5 | 2 | 3 | 4 | 6',
        '```',
        '',
        '这样箭头明确表示的是 **1 和 2 之间、也就是 2 的前面**。',
        '',
        '接着要删除值为 `3` 的元素，此时它位于下标 `3`：',
        '',
        '```cpp',
        'vec.erase(vec.begin() + 3);',
        '```',
        '',
        '删除后得到：',
        '',
        '```cpp',
        '{1, 1.5, 2, 4, 6}',
        '```',
        '',
        '因此练习题中的核心写法是：',
        '',
        '```cpp',
        'vec.insert(vec.begin() + 1, 1.5);',
        'vec.erase(vec.begin() + 3);',
        '```',
        '',
        '###### 插入和删除的时间复杂度',
        '',
        '`std::vector` 的元素连续存储，因此在中间插入或删除时，操作位置之后的元素通常需要移动。常见复杂度可以记为：',
        '',
        '| 操作 | 时间复杂度 |',
        '| --- | --- |',
        '| `vec[i]` | `O(1)` |',
        '| `push_back()` | 摊销 `O(1)` |',
        '| `pop_back()` | `O(1)` |',
        '| 中间 `insert()` | `O(n)` |',
        '| 中间 `erase()` | `O(n)` |',
        '',
        '`push_back()` 之所以是**摊销 `O(1)`**，是因为容量足够时只需在尾部构造一个元素；只有容量不足时才需要重新分配更大的连续存储区域并搬移已有元素。',
        '',
        '> [!IMPORTANT]',
        '> `vector` 的连续存储让随机访问和尾部追加非常高效，但也使中间插入、删除通常需要移动后续元素。',
        '',
        '##### 13.2.4 `clear()` / `shrink_to_fit()`：清空元素与收缩容量',
        '',
        '`clear()` 会删除容器中的全部元素，因此调用后：',
        '',
        '```cpp',
        'vec.clear();',
        '',
        'vec.size() == 0;    // true',
        'vec.empty();        // true',
        '```',
        '',
        '但是 `clear()` **不会主动缩小 `capacity()`**。也就是说，它清除的是“元素”，而不是顺便把已经分配的存储容量释放掉。',
        '',
        '可以把它理解成：',
        '',
        '```text',
        'clear() 前： size = 5, capacity = 8',
        'clear() 后： size = 0, capacity = 8',
        '```',
        '',
        '`shrink_to_fit()` 用于**请求**实现尽量把多余容量释放掉：',
        '',
        '```cpp',
        'vec.shrink_to_fit();',
        '```',
        '',
        '它通常被理解为请求让 `capacity()` 尽量接近 `size()`。例如当前 `size() == 5` 时，某个实现可能在调用后得到 `capacity() == 5`。',
        '',
        '> [!WARNING]',
        '> C++ 标准规定 `shrink_to_fit()` 是一个 **non-binding request（非强制请求）**。实现可以不实际收缩容量，因此通用代码不应依赖 `shrink_to_fit()` 之后一定满足 `capacity() == size()`。练习题如果断言它们相等，实际上依赖了当前标准库实现的行为。',
        '',
        '如果某个实现执行：',
        '',
        '```cpp',
        'vec.shrink_to_fit();  // 假设此时 capacity() 变成 5',
        'vec.clear();',
        '```',
        '',
        '那么 `clear()` 之后通常仍然是：',
        '',
        '```text',
        'size()     == 0',
        'empty()    == true',
        'capacity() == 5',
        '```',
        '',
        '因为 `clear()` 本身不缩减容量。',
        '',
        '可以把几个常见操作对 `size()` 和 `capacity()` 的影响总结为：',
        '',
        '| 操作 | `size()` | `capacity()` |',
        '| --- | --- | --- |',
        '| `clear()` | 变为 `0` | 不主动缩小 |',
        '| `pop_back()` | 减 `1` | 通常不变 |',
        '| `resize(更小)` | 变小 | 通常不变 |',
        '| `reserve(n)` | 不变 | 必要时增大到至少 `n` |',
        '| `shrink_to_fit()` | 不变 | 请求缩小，但不保证成功 |',
        '',
        '参考：[cppreference：`std::vector`](https://en.cppreference.com/w/cpp/container/vector)、[cppreference：`std::vector::size`](https://en.cppreference.com/w/cpp/container/vector/size)、[cppreference：`std::vector::empty`](https://en.cppreference.com/w/cpp/container/vector/empty)、[cppreference：`std::vector::capacity`](https://en.cppreference.com/w/cpp/container/vector/capacity)、[cppreference：`std::vector::data`](https://en.cppreference.com/w/cpp/container/vector/data)、[cppreference：`std::vector::insert`](https://en.cppreference.com/w/cpp/container/vector/insert)、[cppreference：`std::vector::erase`](https://en.cppreference.com/w/cpp/container/vector/erase)、[cppreference：`std::vector::clear`](https://en.cppreference.com/w/cpp/container/vector/clear)、[cppreference：`std::vector::shrink_to_fit`](https://en.cppreference.com/w/cpp/container/vector/shrink_to_fit)。',
        '',
        '[vector-data-marker]: # "#### 13.2 `std::vector::data()`：取得底层连续存储的首地址"',
        ''
    ].join('\n');

    window.fetch = function () {
        var args = arguments;
        var input = args[0];
        var url = typeof input === 'string' ? input : (input && input.url ? input.url : '');

        return previousFetch.apply(window, args).then(function (response) {
            var decodedUrl = url;
            try { decodedUrl = decodeURIComponent(url); }
            catch (error) {}

            if (decodedUrl.indexOf('C++语言学习.md') === -1) return response;

            return response.text().then(function (markdown) {
                if (markdown.indexOf('#### 13.1 `std::array`：固定大小连续容器') === -1) {
                    markdown = markdown.replace(
                        '\n### 14、类型别名声明（using别名）',
                        '\n\n' + chapter13Section + '\n### 14、类型别名声明（using别名）'
                    );
                }

                var headers = new Headers(response.headers);
                headers.delete('content-length');
                return new Response(markdown, {
                    status: response.status,
                    statusText: response.statusText,
                    headers: headers
                });
            });
        });
    };

    var baseScript = document.createElement('script');
    baseScript.src = './cpp-language-page-base.js?v=20260811a';
    baseScript.async = false;
    document.head.appendChild(baseScript);
})();
